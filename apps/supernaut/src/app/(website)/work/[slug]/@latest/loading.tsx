import React from 'react';
// eslint-disable-next-line @nx/enforce-module-boundaries
import {LoadingIndicator} from '@supernaut/shared-ui';
export default function LoadingList() {
  return (
    <LoadingIndicator />
  )
}
